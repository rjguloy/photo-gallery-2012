name: gallery
run-name: Setting up the photo gallery database
on: [push]
jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
      - name: Test output
        run: echo "Here is a test"
  connect-remote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Show database
        run: mysql -h ${{ secrets.DB_HOSTNAME }} --port 3306 -u${{ secrets.DB_USER }} -p${{ secrets.DB_PASS }} -e "SHOW DATABASES"